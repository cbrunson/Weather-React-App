import React from "react";
import logo from "../assets/WeatherChannelLogo.png";

const TopBar = () => {
    return (
        <div className="topBar">
            <img src={logo} />
        </div>
    );
};

export default TopBar;
